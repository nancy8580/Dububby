# Do NOT commit this file to git. It contains local secrets.
# Use 127.0.0.1 to prefer TCP over socket and avoid socket auth ambiguities.
DATABASE_URL="mysql://myapp_user:strongpassword@127.0.0.1:3306/lowcode_dev"
SESSION_SECRET="change_this_to_a_random_secret_please_replace"
# Optional: change the port if your backend uses a different port
# PORT=4000
